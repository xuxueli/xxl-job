<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xxl.job.admin.mapper.XxlJobLogReportMapper">

	<resultMap id="XxlJobLogReport" type="com.xxl.job.admin.model.XxlJobLogReport" >
		<result column="id" property="id" />
		<result column="trigger_day" property="triggerDay" />
		<result column="running_count" property="runningCount" />
		<result column="suc_count" property="sucCount" />
		<result column="fail_count" property="failCount" />
	</resultMap>

	<sql id="Base_Column_List">
		t.id,
		t.trigger_day,
		t.running_count,
		t.suc_count,
		t.fail_count
	</sql>

	<!--<insert id="save" parameterType="com.xxl.job.admin.model.XxlJobLogReport"
			useGeneratedKeys="true" keyProperty="id" keyColumn="id">
		INSERT INTO xxl_job_log_report (
			trigger_day,
			running_count,
			suc_count,
			fail_count
		) VALUES (
					 #{triggerDay},
					 #{runningCount},
					 #{sucCount},
					 #{failCount}
				 )
	</insert>

	<update id="update" >
		UPDATE xxl_job_log_report
		SET running_count = #{runningCount},
			suc_count = #{sucCount},
			fail_count = #{failCount}
		WHERE trigger_day = #{triggerDay}
	</update>-->

	<insert id="saveOrUpdate" parameterType="com.xxl.job.admin.model.XxlJobLogReport"
			useGeneratedKeys="true" keyProperty="id" keyColumn="id">
		INSERT INTO xxl_job_log_report (
			trigger_day,
			running_count,
			suc_count,
			fail_count
		) VALUES (
					 #{triggerDay},
					 #{runningCount},
					 #{sucCount},
					 #{failCount}
				 )
			ON CONFLICT (trigger_day) DO UPDATE
											 SET running_count = EXCLUDED.running_count,
											 suc_count = EXCLUDED.suc_count,
											 fail_count = EXCLUDED.fail_count
	</insert>

	<select id="queryLogReport" resultMap="XxlJobLogReport">
		SELECT <include refid="Base_Column_List" />
		FROM xxl_job_log_report t
		WHERE t.trigger_day BETWEEN #{triggerDayFrom} AND #{triggerDayTo}
		ORDER BY t.trigger_day ASC
	</select>

	<select id="queryLogReportTotal" resultMap="XxlJobLogReport">
		SELECT
			COALESCE(SUM(running_count), 0) AS running_count,
			COALESCE(SUM(suc_count), 0) AS suc_count,
			COALESCE(SUM(fail_count), 0) AS fail_count
		FROM xxl_job_log_report t
	</select>

</mapper>